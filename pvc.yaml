apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openrag-openarchiver-connector-state
  labels:
    app.kubernetes.io/name: openrag-openarchiver-connector
    app.kubernetes.io/part-of: openrag
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
